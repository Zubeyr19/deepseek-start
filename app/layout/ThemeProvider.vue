/*
import { ThemeProvider } from "./context/ThemeContext";

export defualt function Layout({children}: { children: React.ReactNode})  
return (
    <ThemeProvider>
      <div>{children}</div>
    </ThemeProvider>
  ); 
*/
