
/*
import { createRouter, createWebHistory } from 'vue-router'
import AIChat from '../routes/index.vue'

const routes = [
  {
    path: '/',
    name: 'home',
    component: AIChat
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
*/